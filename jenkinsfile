pipeline {
    agent any

    environment {
        PYTHON_VERSION = '3.9'
        VENV_PATH = 'venv'
        PYTHON_PATH = "C:\\Users\\User\\AppData\\Local\\Programs\\Python\\Python310"
    }

    stages {
     //   stage('Checkout') {
     //       steps {
     //           // Clonar el repositorio
     //        //   checkout scm
     //       }
     //   }
  

        stage('Set up Python') {
            steps {
                // Configurar Python y crear un entorno virtual
                bat """
                set PATH=${PYTHON_PATH};%PATH%
                ${PYTHON_PATH}\\python.exe -m venv ${VENV_PATH}
                """
            }
        }

        stage('Install dependencies') {
            steps {
                // Instalar dependencias
                bat """
                set PATH=${PYTHON_PATH};%PATH%
                call ${VENV_PATH}\\Scripts\\activate
                pip install --upgrade pip
                pip install -r requirements.txt
                """
            }
        }

        stage('Run tests') {
            steps {
                // Ejecutar pruebas
                bat """
                set PATH=${PYTHON_PATH};%PATH%
                call ${VENV_PATH}\\Scripts\\activate
                python -m unittest discover -s test
                """
            }
        }

        stage('Deploy') {
            steps {
                script {
                    // Desplegar tu aplicación Flask
                    echo 'Deploying application...'
                    // Ejemplo: bat 'scp -r . user@your_server:/path/to/deploy'
                }
            }
        }
    }
 
}